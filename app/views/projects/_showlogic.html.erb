<script type="text/javascript" deffer>

  $('#toggler').click(function() {
      $('input[type="radio"]').not(':checked').prop("checked", true);
      $('#div1').html($('input[type="radio"]:checked').val())
  });

	// Update the progressbar
  
  $( document ).ready(function() {

  // Check Box 1 -------------------------------------------
  $('.testing').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_one').click(); 
  });

  if ($('input#build_one_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox2').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox2").checked=true;
    }
  }

  // Check Box 2 -------------------------------------------
  $('.testing_2').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_two').click(); 
  });

  if ($('input#build_two_2_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox3').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox3").checked=true;
    }
  }

  // Check Box 3 -------------------------------------------
  $('.testing_3').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_three').click(); 
  });

  if ($('input#build_three_3_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox4').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox4").checked=true;
    }
  }

  // Check Box 4 -------------------------------------------
  $('.testing_4').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_four').click(); 
  });

  if ($('input#build_four_4_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox5').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox5").checked=true;
    }
  }

  // Check Box 5 -------------------------------------------
  $('.testing_5').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_five').click(); 
  });

  if ($('input#build_five_5_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox6').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox6").checked=true;
    }
  }

  // Check Box 6 -------------------------------------------
  $('.testing_6').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_six').click(); 
  });

  if ($('input#build_six_6_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox7').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox7").checked=true;
    }
  }

  // Check Box 7 -------------------------------------------
  $('.testing_7').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_seven').click(); 
  });

  if ($('input#build_seven_7_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox8').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox8").checked=true;
    }
  }

  // Check Box 8 -------------------------------------------
  $('.testing_8').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_eight').click(); 
  });

  if ($('input#build_eight_8_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox9').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox9").checked=true;
    }
  }

  // Check Box 9 -------------------------------------------
  $('.testing_9').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_nine').click(); 
  });

  if ($('input#build_nine_9_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox10').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox10").checked=true;
    }
  }

  // Check Box 10 -------------------------------------------
  $('.testing_10').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_ten').click(); 
  });

  if ($('input#build_ten_10_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox11').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox11").checked=true;
    }
  }

// Check Box 11 -------------------------------------------
  $('.testing_11').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_eleven').click(); 
  });

  if ($('input#build_eleven_11_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox12').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox12").checked=true;
    }
  }

// Check Box 12 -------------------------------------------
  $('.testing_12').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_twelve').click(); 
  });

  if ($('input#build_twelve_12_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox13').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox13").checked=true;
    }
  }

// Check Box 12 -------------------------------------------
  $('.testing_13').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_thirteen').click(); 
  });

  if ($('input#build_thirteen_13_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox14').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox14").checked=true;
    }
  }

// Check Box 12 -------------------------------------------
  $('.testing_14').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_fourteen').click(); 
  });

  if ($('input#build_fourteen_14_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox15').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox15").checked=true;
    }
  }

// Check Box 12 -------------------------------------------
  $('.testing_15').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_fifthteen').click(); 
  });

  if ($('input#build_fifthteen_15_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox16').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox16").checked=true;
    }
  }

// Check Box 16 -------------------------------------------
  $('.testing_16').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_sixteen').click(); 
  });

  if ($('input#build_sixteen_16_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox17').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox17").checked=true;
    }
  }

// Check Box 17 -------------------------------------------
  $('.testing_17').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_seventeen').click(); 
  });

  if ($('input#build_seventeen_17_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox18').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox18").checked=true;
    }
  }

// Check Box 18 -------------------------------------------
  $('.testing_18').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_eighteen').click(); 
  });

  if ($('input#build_eighteen_18_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox19').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox19").checked=true;
    }
  }

// Check Box 19 -------------------------------------------
  $('.testing_19').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_nineteen').click(); 
  });

  if ($('input#build_nineteen_19_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox20').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox20").checked=true;
    }
  }

// Check Box 20 -------------------------------------------
  $('.testing_20').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_twentee').click(); 
  });

  if ($('input#build_twentee_20_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox21').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox21").checked=true;
    }
  }

// Check Box 21 -------------------------------------------
  $('.testing_21').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_twenteeone').click(); 
  });

  if ($('input#build_twenteeone_21_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox22').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox22").checked=true;
    }
  }

// Check Box 22 -------------------------------------------
  $('.testing_22').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_twenteetwo').click(); 
  });

  if ($('input#build_twenteetwo_22_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox23').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox23").checked=true;
    }
  }

// Check Box 23 -------------------------------------------
  $('.testing_23').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_twenteethree').click(); 
  });

  if ($('input#build_twenteethree_23_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox24').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox24").checked=true;
    }
  }

// Check Box 24 -------------------------------------------
  $('.testing_24').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_twenteefour').click(); 
  });

  if ($('input#build_twenteefour_24_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox25').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox25").checked=true;
    }
  }

// Check Box 25 -------------------------------------------
  $('.testing_25').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_twenteefive').click(); 
  });

  if ($('input#build_twenteefive_25_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox26').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox26").checked=true;
    }
  }

// Check Box 26 -------------------------------------------
  $('.testing_26').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_twenteesix').click(); 
  });

  if ($('input#build_twenteesix_26_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox27').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox27").checked=true;
    }
  }

// Check Box 27 -------------------------------------------
  $('.testing_27').click(function() {
    $('input[type="radio"]').not(':checked').prop("checked", true);
    $('#build_twenteeseven').click(); 
  });

  if ($('input#build_twenteeseven_27_2').is(':checked')) {
    var isChecked= document.getElementById('checkbox28').checked;
    if(isChecked){ //checked
      console.log('Not Checked')
    }else{ //unchecked
      document.getElementById("checkbox28").checked=true;
    }
  }







  // Timline Logic -------------------------------------------
	function updateProgress() {
		var startDate = "<%= @project.start_date %>";
		var endDate = "<%= @project.end_date %>";

		if (startDate != "" && endDate != "") {
			var minDate = new Date(convertStringToDate(startDate));
			var today = new Date();
			var maxDate = new Date(convertStringToDate(endDate));

			var nbTotalDays = Math.floor((maxDate.getTime() - minDate.getTime()) / 86400000);
			var nbPastDays = Math.floor((today.getTime() - minDate.getTime()) / 86400000);

			var percent = nbPastDays / nbTotalDays * 100;

			// Extreme cases
			if (percent < 0) {
				percent = 0;
			} else if (percent > 100) {
				percent = 100;
			}
		
			$("#progressbar").reportprogress(percent);
		}
	}
  
  
    updateProgress();
  });
  </script>